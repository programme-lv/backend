
// List all tasks
// func (s *TaskService) ListTasks(ctx context.Context) (res []*Task, err error) {
// 	var response *dynamodb.ScanOutput
// 	scanPaginator := dynamodb.NewScanPaginator(s.ddbClient, &dynamodb.ScanInput{
// 		TableName: &s.taskTableName,
// 	var all []*TaskRow
// 	err := s.ddbClient
// 	if err != nil {
// 		return nil, err
// 	}

// 	res = make([]*Task, 0)

// 	for _, row := range all {
// 		task, err := ddbTaskRowToResponse(row)
// 		if err != nil {
// 			return nil, fmt.Errorf("could not convert task row to response: %w", err)
// 		}
// 		res = append(res, task)
// 	}

// 	return
// }

// func (s *TaskService) CreateTask